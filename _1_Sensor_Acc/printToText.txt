PrintWriter output;

void setup() {
	output = creatWriter("data.csv");
}

void loop() {

	whild(Serial.available() > 0) {
	String value = Serial.readString();

		if (value != null) {
		output.println(value);
		}
	}	
}